name: CD

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: [self-hosted, linux]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Restart app
        run: |
          docker-compose down || true
          docker-compose up -d --build